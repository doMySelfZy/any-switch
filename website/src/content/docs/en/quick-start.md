---
title: Quick start
description: Add a site, fetch models, apply to Claude Code, Codex, Pi, or Prime, then restart the CLI.
order: 3
---

1. Add an upstream site with a name, Base URL, and API key
2. Fetch models, or type the model ids you need
3. Open Apply Center, pick Claude Code, Codex, Pi, or Prime, confirm the model and options, then apply
4. Restart the terminal or reopen the matching CLI so the change takes effect

A site can have multiple routes; **the first one is the current default**, and you can probe and switch at any time.

## First launch

If there are no sites yet, the Sites page walks you through adding one. The site list shows only a key prefix. Editing a site reads the full value from encrypted storage into a password field that is hidden by default.

After a successful apply, the status card shows the site and model. If the CLI still uses the old URL, confirm step 4.
