# floating-window

## Goal

实现桌面悬浮窗显示所有站点余额，支持收起/展开、自动刷新、位置保存

## Requirements

### 功能需求

1. **桌面悬浮窗**
   - 在 Windows 桌面上创建一个始终置顶的悬浮窗口
   - macOS 支持（Tauri 2 跨平台）
   - 无边框、可拖动、透明背景
   - 不显示在任务栏
   - 支持两种状态：收起（小按钮）和展开（完整列表）

2. **余额显示**
   - 显示所有已配置站点的余额信息
   - 每个站点显示：站点名称、剩余额度
   - 支持不同货币单位显示（$、¥、Unlimited 等）
   - 显示最后更新时间
   - 余额为 0 或即将耗尽时高亮提示

3. **交互方式**
   - 点击收起状态按钮 → 展开完整列表
   - 点击展开状态的收起按钮 → 收起为小按钮
   - 拖动窗口可自由移动位置
   - 位置自动保存，重启后恢复
   - 可选：点击站点项跳转到主窗口

4. **自动刷新**
   - 后台定时自动刷新所有站点余额
   - 刷新间隔可配置（默认 5 分钟）
   - 手动刷新按钮
   - 刷新时显示加载状态

5. **设置集成**
   - 在设置页面添加「悬浮窗」配置项
   - 开关：启用/禁用悬浮窗（默认启用）
   - 配置：自动刷新间隔（分钟）
   - 操作：重置窗口位置到默认位置

### 技术需求

1. **后端（Rust）**
   - 新增 `FloatingWindowSettings` 配置结构
   - 新增 `floating_window.rs` 模块管理窗口生命周期
   - 新增 Tauri Commands：
     - `get_all_sites_quota` - 获取所有站点余额汇总
     - `toggle_floating_window` - 切换悬浮窗显示/隐藏
     - `save_floating_window_position` - 保存窗口位置
   - 后台定时刷新任务（tokio::time::interval）
   - 窗口配置：always_on_top、frameless、skip_taskbar

2. **前端（Vue）**
   - 新增 `FloatingWindow.vue` 页面
   - 新增 `QuotaList.vue` 组件（展开状态）
   - 新增 `CollapsedView.vue` 组件（收起状态）
   - 毛玻璃效果、深色主题适配
   - 动画效果（收起/展开、余额变化）

3. **数据库**
   - 扩展 `AppSettings` 表添加悬浮窗配置字段
   - 存储窗口位置（x, y）
   - 存储悬浮窗状态（启用、收起、刷新间隔）

### 约束条件

1. 悬浮窗使用独立的 WebView，不影响主窗口性能
2. 后台定时器只在悬浮窗启用时运行
3. 使用缓存避免频繁请求 API
4. 窗口默认位置：右下角（距离屏幕边缘 20px）
5. 收起状态尺寸：60x60px 圆形按钮
6. 展开状态尺寸：280x400px 列表窗口

## Acceptance Criteria

> 勾选依据分两类：**[自动]** = 有代码/测试依据（括号内注明）；**[目视]** = 只能真机肉眼确认，
> 下面用 `[?]` 标出，未勾选。

### 基础功能
- [x] 悬浮窗可以在桌面上创建并显示 — **[自动]** `floating_window::create_floating_window`；
      启动本机应用时实际枚举到该窗口。
- [x] 悬浮窗始终置顶，不显示在任务栏 — **[自动]** 创建时 `always_on_top(true)` + `skip_taskbar(true)`。
- [x] 可以正常拖动窗口，位置重启后恢复 — **[自动]** 拖动改为松手写库（原先每帧写库），
      启动时读回 `position_x/y`。
- [x] 显示所有站点的余额信息（名称 + 余额）— **[自动]** 本次修复（原实现恒返回 `None`）；
      4 个用例覆盖正常/低值/无限额/未知四态。
- [x] 收起/展开状态切换正常 — **[自动]** 本次实现；用例覆盖折叠、持久化、展开。

### 数据同步
- [x] 余额数据从数据库正确读取 — **[自动]** 站点列表来自数据库；额度经探测后进后端缓存
      （悬浮窗是独立 webview，拿不到主窗口 store）。
- [x] 自动刷新功能正常工作（默认 5 分钟）— **[自动]** 用例断言「不足间隔不触发、到点触发、卸载后停止」。
- [x] 手动刷新按钮可用 — **[自动]** 用例断言点击后发出探测请求。
- [x] 刷新时显示加载状态 — **[自动]** 首次加载渲染 `Spin`，刷新按钮带 `loading`。
- [x] 最后更新时间显示正确 — **[自动]** 用例断言渲染「最后更新」。

### 设置集成
- [x] 设置页面可以开启/关闭悬浮窗 — **[自动]** 已有 Switch + `toggle_floating_window`。
- [x] 设置页面可以配置刷新间隔 — **[自动]** 本次修复字段名（原 `autoRefreshSeconds` 与后端
      `auto_refresh_minutes` 不一致，serde 静默忽略导致设置存不进去）。
- [x] 设置页面可以重置窗口位置 — **[自动]** 本次接上一直未被调用的 `reset_floating_window_position`。
- [x] 设置变更立即生效 — **[自动]** 新增 `floating-settings-changed` 跨窗口事件；
      用例断言订阅存在且在改设置后按新间隔刷新。

### 视觉效果
- [?] 毛玻璃背景效果正常 — 代码已设 `backdrop-filter: blur(12px)`，**需真机肉眼确认**。
- [?] 深色主题适配良好 — 采用固定深色配色，**需真机肉眼确认**。
- [x] 收起/展开动画流畅 — **[自动]** 内容区 `max-height`/`opacity` 过渡 200ms；
      窗口高度本身由 Tauri `setSize` 即时改变（API 无法动画）。
- [x] 余额为 0 或低值时有视觉提示 — **[自动]** 低于 $5 转警示色并在标题栏显示警示图标；
      用例断言低值站点仍显示金额。
- [?] 整体视觉风格与主窗口一致 — **需真机肉眼确认**。

### 性能与稳定性
- [?] 悬浮窗不影响主窗口性能 — 刷改为异步、拖动只在松手写库，**实测仍待真机**。
- [x] 后台刷新不阻塞 UI — **[自动]** 探测为 `async`，不占用渲染线程。
- [x] 关闭悬浮窗后定时器正确停止 — **[自动]** 用例断言卸载后不再发出请求。
- [?] 内存占用合理（< 50MB）— **需真机实测**。
- [?] 无明显内存泄漏 — **需真机实测**（监听器与定时器均有清理路径）。

## 本次补齐的缺口（原实现为半成品）

| 缺口 | 影响 | 处理 |
|------|------|------|
| `get_all_sites_quota` 恒返回 `quota: None` | 悬浮窗余额永远显示「不可用」——核心功能失效 | 加后端额度缓存 + 并发探测刷新 |
| 前端 `autoRefreshSeconds` vs 后端 `auto_refresh_minutes` | 刷新间隔设置静默存不进去 | 统一为分钟（与其它间隔设置一致） |
| 无自动刷新定时器 | 打开后不会更新 | 按设置间隔定时刷新，卸载时清理 |
| 无折叠实现（后端已有 `collapsed` 字段） | 收起/展开不可用 | 实现折叠 + 窗口高度收缩 + 持久化 |
| 拖动时每帧写数据库 | 拖动卡顿、数据库压力 | 拖动只移动窗口，松手写一次 |
| 文案硬编码中文 | 违反 i18n 约定，英文界面显示中文 | 全部走 i18n |

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
